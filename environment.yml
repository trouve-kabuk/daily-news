name: daily-news

channels:
  - conda-forge

dependencies:
  - python=3.13
  - pip
  - pip:
      - "mlx-lm; sys_platform == 'darwin' and platform_machine == 'arm64'"
      - "llama-cpp-python>=0.3.16,<1; sys_platform == 'darwin' and platform_machine == 'arm64'"
